import InterviewClient from './InterviewClient';

export default function InterviewPage({ params }: { params: { interview_id: string } }) {
  return <InterviewClient interviewId={params.interview_id} />;
}
